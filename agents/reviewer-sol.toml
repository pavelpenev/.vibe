agent_type = "subagent"
display_name = "Reviewer (Sol)"
description = "Independent artifact reviewer on GPT-5.6-sol (strongest tier); read-only review of code, docs, specs, plans — used for deep reviews and plans"
safety = "safe"
active_model = "gpt-5.6-sol"
bypass_tool_permissions = false
system_prompt_id = "reviewer"
enabled_tools = ["read_file", "grep", "bash"]

[tools.read_file]
permission = "always"

[tools.grep]
permission = "always"

[tools.bash]
permission = "always"
allowlist = []
denylist = [
    "git push",
    "git reset --hard",
    "git clean",
    "git checkout",
    "git stash drop",
    "rm -rf",
    "sudo",
]
sensitive_patterns = [
    "sudo",
]
denylist_standalone = [
    "python",
    "python3",
    "ipython",
    "bash",
    "sh",
    "nohup",
    "vi",
    "vim",
    "emacs",
    "nano",
    "su",
]
